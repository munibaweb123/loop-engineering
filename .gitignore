# Python bytecode from running the project scripts
__pycache__/
*.pyc

# The spine — per-student memory each project writes at runtime, not source
progress.md

# Per-student run history a loop writes at runtime, not source
run.log

# Per-run debug output from docs-freshness/run-beat.sh -- not source
docs-freshness/.maker-*.json
docs-freshness/.review-*.json
docs-freshness/.last-error.log
docs-freshness/.cron.log
