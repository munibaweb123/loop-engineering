# Python bytecode from running the project scripts
__pycache__/
*.pyc

# The spine — per-student memory each project writes at runtime, not source
progress.md

# Per-student run history a loop writes at runtime, not source
run.log
