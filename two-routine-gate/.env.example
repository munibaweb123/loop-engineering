# Routine B authentication token
# Generate a random token: openssl rand -hex 32
APPROVAL_TOKEN=

# Tracking issue number for the digest
TRACKING_ISSUE=1

# Server port (optional, defaults to 8080)
PORT=8080
